[pytest]
markers =
    gcp: requires GCP adapter to run

# Pending upstream tests that still target post-v8.2.0 module names live in
# test/_pending_v8_adaption/ and are excluded from collection.
norecursedirs = _pending_v8_adaption build dist .venv venv
